UI:
    Have the player name (h1) and points (h4) on top / Done
    Have the number of active players / Done
    Have a ready button with the number of players ready and total number of players / Done
    A chat in the bottom

Ready:
    User clicks ready

    Javascript sends the ready event to the server

    If user already ready:
        server removes player from ready

        Javascript updates the UI (ready button becomes green and text ready)

    If user not ready:
        server adds player to ready

        Javascript updates the UI (ready button becomes red and text unready)

Track Users:
    When a user logs in:
        Gets added to active players list / Done
        Gets a generator that always updates(every 0.07s): / Done
            Ready players
            Active players

    When generator exits then the user has disconnected from lobby:
        Went to game (Handled seperately)
        Disconnected (The normal case) / Done
